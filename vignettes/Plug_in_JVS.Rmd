---
title: "Plug-in JVS"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Plug_in_JVS}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(JDCruncheR)
library(rjwsacruncher)
```

## Introduction

This vignette explains how to create a JVS Quality Report with the rjd3qr package. 

## Creation of demetra_m.csv

To create a JVS Quality Report, you need to use the Cruncher with specific options, using the rjwsacruncher package. If you do not have downloaded the JDemetra+ Cruncher, you can download it from github : https://github.com/jdemetra/jdplus-main/releases. The file you should download starts with "jwsacruncher-standalone". 

The goal of this step is to generate a demetra_m file containing a lot of informations on series to study, and also several files containing the components of the series.

To generate those files, you need to use the Cruncher with the following options :

```{r}
options(
    is_cruncher_v3 = TRUE,
    default_matrix_item = c(
        "span.start",
        "span.end",
        "m-statistics.m7",
        "span.n",
        "regression.leaster", "regression.ntd", "regression.lp",
        "regression.out(*)",
        "diagnostics.seas-lin-combined", "regression.td-ftest:3",
        "log",
        "arima.p", "arima.d", "arima.q", "arima.bp", "arima.bd", "arima.bq",
        "regression.nout",
        "diagnostics.seas-sa-f:2", "diagnostics.td-sa-last:2",
        "m-statistics.q", "m-statistics.q-m2",
        "decomposition.trend-filter",
        "decomposition.d7-trend-filter", "decomposition.seasonal-filters",
        "quality.summary",
        "diagnostics.seas-sa-ac1:3",
        "diagnostics.seas-sa-ac1", "residuals.lb:3"
    ),
    default_tsmatrix_series = c(
        "y", "sa", "s", "i", "t",
        "decomposition.y_cmp",
        "decomposition.s_cmp",
        "decomposition.i_cmp", "decomposition.i_lin",
        "decomposition.sa_cmp", "decomposition.t_cmp",
        "t_cmp", "sa_cmp"
    )
)

rjwsacruncher::cruncher_and_param(
    workspace = ".../inst/extdata/WS/WS_world.xml",
    cruncher_bin_directory = ".../jwsacruncher-3.7.1/bin",
    csv_layout = 'vtable',
    short_column_headers = FALSE,
    refreshall = FALSE,
    v3 = TRUE,
    delete_existing_file = TRUE,
    policy = "None"
)

```

At this step, you should have generated 11 files.

## Creation of report JVS

Once the Cruncher has generated the right outputs, the JVS Quality Report can be generated.

```{r}
out_dir <- ".../inst/extdata/WS/WS_world/Output/SAProcessing-1"
JVS <- extract_JVS(dir = out_dir)
```

The JVS object is a dataframe containing the quality report. To save it, you need to export it.

## Export

To export the JVS Quality Report, you need to use the writing function. The report can be exported as a csv or an xlsx file.

```{r}
write(JVS, format = "csv", export_dir = "...", overwrite = TRUE) # or format = "xlsx"
```
